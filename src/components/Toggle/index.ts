import Root from './Toggle.svelte';

/** Pressed-state toggle button (bits-ui v2 Toggle). Single part: `Toggle.Root` — `pressed` two-way bindable, `data-state` on|off, built on the kit's button-core. */
export const Toggle = { Root };

export type { ToggleProps } from './Toggle.svelte';
